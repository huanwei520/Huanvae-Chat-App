# ============================================
# Huanvae Chat App 发布配置
# ============================================
#
# 使用方法:
#   1. 修改 VERSION 为要发布的版本号
#   2. 修改 MESSAGE 为本次更新说明
#   3. 运行 ./scripts/linux/release.sh
#
# 发布流程:
#   1. 脚本会检查当前项目版本号一致性
#   2. 如果版本不同，自动更新所有配置文件
#   3. 运行完整测试（必须全部通过）
#   4. Git 提交 + 创建标签 + 推送
#
# ============================================

VERSION=1.1.36
MESSAGE=Windows VPN 服务修复（A+C+F1+F3）。① A 修复按钮：VPN 页在 Windows 上新增「安装/修复服务」按钮，提权注册并启动服务，注册/授权/启动三步各判返回码、各有独立退出码，失败在界面上说清失败在哪一步；UAC 点「否」单独一类（ERROR_CANCELLED 1223）且不卡转圈；提权进程 rc=0 之后父进程不信其自述、回头问 SCM 最终态（sc start 返回 0 只代表 SCM 收下请求，起来又立刻死只有这里露馅）。② C 安装器从源头改：bundle.windows.nsis.installMode=perMachine 让安装器提权（服务注册才可能成功）；hooks.nsi 每条 sc.exe 后 Pop 取 rc，create/sdset/start 三条进条件判断，致命失败各配 MessageBox；删掉原来那句无条件 DetailPrint「HuanvaeGuard 服务已安装」——那正是「失败也显示成功」的源头。③ perMachine 的连带修复（原方案未列、不修则上线即废掉整个 App）：安装位置变 Program Files 后 portable 布局的 data 目录不可写，user_data.rs 新增 Windows 分支把数据根重定向到 %LOCALAPPDATA%\Huanvae-Chat-App\data，目录名取自 productName，与旧 currentUser 安装的数据路径逐字相同，存量用户原地接上旧数据、零迁移代码。④ 升级路径：hooks.nsi PREINSTALL 剥引号后再取父目录并判 rc（原写法因 UninstallString 带引号而静默失效）；RMDir 改非递归，避免清掉新数据根的父目录。⑤ 三处 installMode 守卫口径统一：pre-release.ps1 与 test-all.{ps1,sh} 一律要求 updater.installMode 显式为 passive、nsis.installMode 为 perMachine；依据是上游 tauri-plugin-updater 把 Passive 标成 #[default]，删掉该键行为不变、只是看不见，旧守卫「必须移除」在做一件它做不到的事。同批把两处 ⚠ WARN 升为 FAIL（只打黄字的守卫等于没有守卫），并把 installMode 判定从纯文本 grep 改为结构化取键（该键名在 conf 里出现两次、语义不同，grep 是假阳判据）。⑥ sc query 非零不再一律读成「未安装」（把「查询失败」和「服务不存在」混成一个读数，而两者处置相反），以 1060 为「不存在」的判据。验证与如实披露：核心验收「故意让服务注册失败一次」已在真 Windows Server 2025 上跑通——注入 sc create rc=0 时假 sc 收到 6 条命令（含 description/sdset/start），注入 rc=5 时只收到 3 条、其后全部不执行，即进入报错分支而非静默成功，两侧形状不同故判据有区分力；hooks.nsi 经真 makensis 3.12 编译 rc=0（故意写坏 EndIf 则 rc=1）；pre-release.ps1 经真 pwsh 7.6.3 执行 6/7 （唯一 FAIL 是交互式人工确认拿不到输入）；两条 installMode 守卫做过变异自证且精准只红对应那一条；生成的提权 PowerShell 经真解析器校验 0 语法错误（负对照删一个花括号则 1 错误）。🔴 未验证项，不写成已验收：Windows 真机上的 UAC 弹框行为、A 按钮的端到端点击、C 的真安装/真升级（本轮无 Windows 安装包构建）、以及整包 cargo check --target x86_64-pc-windows-msvc（macOS 无 MSVC 工具链，依赖层 ring 即失败，未走到本仓代码；Windows 分支的类型检查由 x86_64-pc-windows-gnu 逐文件 rustc 承担，配变异自证）。
