# ============================================
# Huanvae Chat App 发布配置
# ============================================
#
# 使用方法:
#   1. 修改 VERSION 为要发布的版本号
#   2. 修改 MESSAGE 为本次更新说明
#   3. 运行 ./scripts/linux/release.sh
#
# 发布流程:
#   1. 脚本会检查当前项目版本号一致性
#   2. 如果版本不同，自动更新所有配置文件
#   3. 运行完整测试（必须全部通过）
#   4. Git 提交 + 创建标签 + 推送
#
# ============================================

VERSION=1.1.29
MESSAGE=手势导航下的返回终于对了 —— 他人资料页/群详情/群设置面板，返回手势与返回键都退回上一层，不再退出 App（v1.1.28 那次只在 3 键导航下有效，手势导航是安卓默认，等于没解决；真因是 React effect 自下而上执行，浮层作为子组件恒早于页面注册，在「后注册者优先」的栈里反而垫底 ⇒ 新增浮层车道，恒先于页面级栈被询问；边缘带几何一个字没动）+ 会话内查找定位窗口化（跳到很早的消息不再把中间几百条整段读进来，只取锚点前后各 30）+ 查找结果图片/视频改方形九宫格封面 + 左键打开预览、右键(长按)才定位 + 一键回到最新按钮（含窗口态恒显）+ 群主/管理员终于能在 App 里审批入群申请（后端三个端点一直都在、客户端从未接过，此前申请永久 pending）+ 下载器 h2 流控窗口调大（单流 1.62x，独立复现；实测 http2_adaptive_window 反而更差故不开）+ 清掉零调用方的死 API「邀请码加群」。测试面：修掉全量负载下 AnimatePresence 退场竞态（asyncUtilTimeout 1000→5000，断言未弱化、变异验证仍咬得住）；修掉一条「名字与实现相反且在缺陷下恒 PASS」的误导性用例；DB 层把「查询出错」与「锚点不存在」拆成两条出口。验收：39 张手势导航真机图（三浮层×两触发路径+负对照+3键正对照），clippy 桌面 0 告警。已知未做：clippy Android 超时未跑完(登记SKIP不算通过)、桌面端截图、iOS、相册发送侧入口。
