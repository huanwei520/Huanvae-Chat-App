# ============================================
# Huanvae Chat App 发布配置
# ============================================
#
# 使用方法:
#   1. 修改 VERSION 为要发布的版本号
#   2. 修改 MESSAGE 为本次更新说明
#   3. 运行 ./scripts/linux/release.sh
#
# 发布流程:
#   1. 脚本会检查当前项目版本号一致性
#   2. 如果版本不同，自动更新所有配置文件
#   3. 运行完整测试（必须全部通过）
#   4. Git 提交 + 创建标签 + 推送
#
# ============================================

VERSION=1.1.45
MESSAGE=v1.1.45: 发布缺口修复 + 13 项周期功能首次正式发布。本版相对 v1.1.44 的代码增量：①会议移动端底栏小屏防裁切（gap/min-width 自适应+wrap，360px 屏 5 控件不溢出，≥437px 观感不变）；②会议内远程控制正式入口（devGate.isRemoteControlEnabled 分离 dev 自动化面：控制窗路由/tile右键/授权桥/WS上行/信令分发常规构建可用，M1 target_user_id 取 tile 真值、无目标不盲发；dev-only 面保持 VITE_DEV_CONTROL 门控）。验证（发布块 cwsecdh0 第2轮，全本体退出码）：typecheck RC=0；ESLint 0/0；vitest 379 文件全过；E2E 41/41 RC=0；pnpm build 0 警告（test-all.sh 第8项判据修复后口径）；cargo check RC=0；clippy 桌面+Android -D warnings 0 警告；cargo test --lib 193/193。结构性差异沿 v1.1.40-v1.1.44 登记照旧：release.sh 步骤3 build-hg-binaries.sh 本宿主不可跑（macOS 宿主专用），本次发货 VPN 二进制与 v1.1.44 resources 落点逐字节一致（git diff v1.1.44..HEAD -- src-tauri/resources/ 为空，manifest 记 b5e6d46）；插件 jniLibs 三 ABI 与 resources arm64 的既有不一致已实证归档（发布块 evidence），修复阻塞在 mTLS 私钥读权。发布机 Linux x86_64；版本/tag 事务在 GIT_DIR 副本构造（.git 目录锁定），push 待凭据注入。