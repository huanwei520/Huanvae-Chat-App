# ============================================
# Huanvae Chat App 发布配置
# ============================================
#
# 使用方法:
#   1. 修改 VERSION 为要发布的版本号
#   2. 修改 MESSAGE 为本次更新说明
#   3. 运行 ./scripts/linux/release.sh
#
# 发布流程:
#   1. 脚本会检查当前项目版本号一致性
#   2. 如果版本不同，自动更新所有配置文件
#   3. 运行完整测试（必须全部通过）
#   4. Git 提交 + 创建标签 + 推送
#
# ============================================

VERSION=1.1.40
MESSAGE=v1.1.40: 版本例行 +0.0.1（owner 明令，流水线块 1789185311119-1）。本版相对 v1.1.39 的代码增量 = 一个已过审 commit：会议音频输入/输出双设备选择真实贯通（桌面端设置页 ↔ 会议窗热切换，原 79ce79a，rebase 后 7a056b2）。发布机为 Linux x86_64（Ubuntu 24.04 内核 6.8），与历史 mac/Windows 发布机不同，两处结构性差异如实登记：① release.sh 步骤 3（build-hg-binaries.sh）在本宿主不可跑——macOS 腿硬要求 Darwin（非 Darwin 即 exit 1），Windows 腿需 HG_WIN_BUILD_HOST（无默认值、本机未注入）；且 HuanvaeGuard 源码仓当前 HEAD 已越过 v1.1.39 manifest 的来源 commit 且带在飞脏改，强行重建等于把未评审在飞代码发出去。故本次发货 VPN 二进制与 v1.1.39 逐字节一致：hg-macos/libhg_android.so 的 sha256 与 hg-build-manifest.json 对账相等，git diff v1.1.39..HEAD -- src-tauri/resources/ 为空。② 门禁 test-all.sh 真跑 12/13：第 13 项 VPN 连通性在 Linux 恒退 3=未执行（取数手段仅覆盖 macOS/Windows），经 ALLOW_SKIP=vpn-connectivity 显式放行并如实登记；第 11 项 Android clippy 由本机 NDK 真跑通过（未走放行）。桌面（pnpm tauri build）与 Android（pnpm tauri android build --apk）双轨本机构建绿灯；签名产物由 tag 触发的 .github/workflows/release.yml 产出（gate → build×3 + build-android → manifest → R2）。🔴 未验证项，不写成已验收：第 13 项真跑 0/5；Windows/macOS 真机零验证；本机 Android APK 无发布签名（签名在 CI），CI 产物与更新清单待 tag 推出后复核。
NOTE-ACTUAL=实际代码增量勘误：feature commit 7a056b2（会议音频双设备选择）在 05:17Z 复核中发现 standalone-red（无门禁绿灯即入发布），已摘除并保全到分支 inflight/meeting-audio-7a056b2；v1.1.40 相对 v1.1.39 的真实增量 = ebd5f50 一个 commit（清零发布门禁 cargo 警告 5 条，零行为变更）。上方 MESSAGE 为 05:13Z 预写版本，以本条勘误为准。
