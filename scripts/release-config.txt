# ============================================
# Huanvae Chat App 发布配置
# ============================================
#
# 使用方法:
#   1. 修改 VERSION 为要发布的版本号
#   2. 修改 MESSAGE 为本次更新说明
#   3. 运行 ./scripts/linux/release.sh
#
# 发布流程:
#   1. 脚本会检查当前项目版本号一致性
#   2. 如果版本不同，自动更新所有配置文件
#   3. 运行完整测试（必须全部通过）
#   4. Git 提交 + 创建标签 + 推送
#
# ============================================

VERSION=1.1.38
MESSAGE=v1.1.38: 加群三开关 App 端接入 + 群二维码加入路径 + 生产六格真界面实测 + 规则沉淀。① 接入后端加群路径三开关（allow_join_via_qr / allow_join_via_search / allow_join_via_referral，migration 045）：applyToJoinGroup 的 source 收成必填位置参数（qr/search/referral），群详情、全局搜索、群卡片分享各入口落地，被关闭的路径在 UI 层拦截并提示；JoinPolicyForm 仅群主可改（PUT /api/groups/{id}/join-policy）。② 群二维码出码与扫码落地页（新增 GroupQrView 菜单视图与 group-qr.css，引入 qrcode + @types/qrcode 依赖；qr_show_scope 管谁能把码拿出去、allow_join_via_qr 管拿到码的人能不能进，两者正交）。③ 生产环境六格真界面截图实测 + 五次翻转服务端读数复核通过（本 run 单1 code / 单2 review 均已 PASS）。④ gen-33/34 规则沉淀以 append-only 追加进 .claude 四个文档（common/frontend-test/rust-dev/release-SKILL），零行号位移。🔴 未验证项，不写成已验收：Windows / Android / iOS 端零真机验证；生产实测只覆盖 macOS 一端的六格界面；视觉回归基线仍只有 linux 一套（darwin 非权威平台，截图断言整类 skip）。
