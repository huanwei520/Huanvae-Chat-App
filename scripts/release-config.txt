# ============================================
# Huanvae Chat App 发布配置
# ============================================
#
# 使用方法:
#   1. 修改 VERSION 为要发布的版本号
#   2. 修改 MESSAGE 为本次更新说明
#   3. 运行 ./scripts/linux/release.sh
#
# 发布流程:
#   1. 脚本会检查当前项目版本号一致性
#   2. 如果版本不同，自动更新所有配置文件
#   3. 运行完整测试（必须全部通过）
#   4. Git 提交 + 创建标签 + 推送
#
# ============================================

VERSION=1.1.23
MESSAGE=更新下载改用自建分片并发下载器：Range 8 段并发 + 断点续传 + 失败重试 + 超时，取代插件默认的单连接顺序下载(受控丢包实测 5%丢包/250ms 下快 4.39 倍)；签名校验原样保留(自己下载绕过了插件内置验签，已用同一套 minisign 复刻，校验不过绝不安装)；更新源只保留 store.huanvae.cn，去掉 GitHub 直连 endpoint；下载进度在总长未知时走不定态而不是钉死 0%。本版只含这一个功能
