VERSION=1.1.52
MESSAGE=三端 hv-control-daemon 正式随包：macOS 腿恢复 externalBin（自 v1.1.46 置空后首次恢复，aarch64/x86_64-apple-darwin 双形态真机构建入库）+ Windows msvc/Linux gnu 原生重建留证（4r2sli2c 块）；NSIS L1 daemon FAIL→PASS 对照在案（1.1.50 包缺件 vs 集成分支包 a907df12 字节一致）。guard 腿源锚 b1d7c8df 未变、resources 与 manifest 逐字节复核一致，按 v1.1.49 登记惯例不重建（HuanvaeGuard HEAD 已属在飞块脏源，发货必须排除）。vpn-connectivity Linux 宿主结构性不可执行，登记式放行（脚本仅覆盖 macOS 侧）。