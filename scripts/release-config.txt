# ============================================
# Huanvae Chat App 发布配置
# ============================================
#
# 使用方法:
#   1. 修改 VERSION 为要发布的版本号
#   2. 修改 MESSAGE 为本次更新说明
#   3. 运行 ./scripts/linux/release.sh
#
# 发布流程:
#   1. 脚本会检查当前项目版本号一致性
#   2. 如果版本不同，自动更新所有配置文件
#   3. 运行完整测试（必须全部通过）
#   4. Git 提交 + 创建标签 + 推送
#
# ============================================

VERSION=1.1.20
MESSAGE=HuanvaeGuard 用户 VPN:App 点「连接」真机验证通过(L3,隧道 down 到 up、10/10 零丢包、隧道字节增量逐字节吻合);修 HuanvaeGuardPage 两处写死 19198 的用户可见日志与头注释,改为打实际解析出的端口;新增端口解析静态契约测试
