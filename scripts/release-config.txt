# ============================================
# Huanvae Chat App 发布配置
# ============================================
#
# 使用方法:
#   1. 修改 VERSION 为要发布的版本号
#   2. 修改 MESSAGE 为本次更新说明
#   3. 运行 ./scripts/linux/release.sh
#
# 发布流程:
#   1. 脚本会检查当前项目版本号一致性
#   2. 如果版本不同，自动更新所有配置文件
#   3. 运行完整测试（必须全部通过）
#   4. Git 提交 + 创建标签 + 推送
#
# ============================================

VERSION=1.1.21
MESSAGE=修两个 VPN P0:macOS「安装/修复」恒失败(launchd override 禁用,bootstrap 前补 launchctl enable);Windows 发货守护进程换成真机验证过能被 SCM 拉起并建隧道的构建。发布流程新增:发布前从 HuanvaeGuard 源码构建各平台二进制并替换校验 sha、测试流程加 VPN 连通性硬门禁(握手+双向字节+端到端 ping)。已知未修:App WS 实时推送失效(红点不更新),将于 v1.1.22 单独发
