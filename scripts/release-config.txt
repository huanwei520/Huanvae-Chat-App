# ============================================
# Huanvae Chat App 发布配置
# ============================================
#
# 使用方法:
#   1. 修改 VERSION 为要发布的版本号
#   2. 修改 MESSAGE 为本次更新说明
#   3. 运行 ./scripts/linux/release.sh
#
# 发布流程:
#   1. 脚本会检查当前项目版本号一致性
#   2. 如果版本不同，自动更新所有配置文件
#   3. 运行完整测试（必须全部通过）
#   4. Git 提交 + 创建标签 + 推送
#
# ============================================

VERSION=1.1.37
MESSAGE=v1.1.37: 外部审计整改 + 视觉门禁复活 + 低代码前端切除 + 发布门禁 + VPN 控制面接线。① 外部审计段一 30 条 critical/high 逐条验证、修其中 24 条（三批 10/8、11/8、9/8），未修的 6 条各自写明为何不成立或不属本仓。② 视觉回归门禁从「一侧恒绿一侧恒红」改成会响的门：权威平台判定改为「有没有入仓基线」（现只有 linux 一套），非权威平台显式 skip 并打印原因、不再产出空绿；基线于 2026-08-21 重置，并写死空白区间声明——自 2026-05-11 起约 164 个 src commit 期间的视觉回归本门禁未覆盖且不可追溯，两向都不可推。删掉 4 条 emulateMedia colorScheme 假覆盖（本 App 主题默认写死 light、根本不查媒体特性，那 4 条渲染的是同一张图，名字却承诺了 light/dark），新增重复基线登记表守卫防复发；e2e 目录纳入 typecheck 与 lint:strict（负对照证明纳入前那两个错结构上看不见），既有 24 error 逐条改代码修掉、真正查质量的规则一条没放宽。③ 砍掉低代码前端整块（39 删 / 29 改 / 2 新增），共享确认框搬家并救出其样式；删除面用类名交集做主防线（可穷举可复算），截图只作抽查。④ release.yml 新增 gate job（typecheck → lint:strict → test:run → @gate e2e），build/build-android/generate-manifest 全部经 needs 挂在它后面，门红即零产物零分发。⑤ VPN 控制面接线：startTunnel 带上 control 凭据（master_url/device_id/access_token/refresh_token），缺必需值中止连接而不静默降级——守护进程据此能 claim/release，用户断开时当帧下线，而不是等 150s/300s 老化。⑥ gen-30 规则沉淀 9 条（判据陷阱 5 条 + 前端样式 4 条）。🔴 未验证项，不写成已验收：VPN「当帧下线」的端到端未验——需要装了本版的载体、有效用户令牌、已装并在跑的守护进程、真隧道、master 侧 status 读数通道，以及造出「它没当帧下线」那一侧的正对照能力，五样缺一即不成立，本版只证到源码层与发货件层；8 张视觉基线本次是第一次在真 CI（ubuntu）上被验，此前 CI 那一侧长期是红的，若红在视觉比对属基线重置后的待确认项；Windows 与手机端零真机验证。
