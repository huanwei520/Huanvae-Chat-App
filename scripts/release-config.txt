# ============================================
# Huanvae Chat App 发布配置
# ============================================
#
# 使用方法:
#   1. 修改 VERSION 为要发布的版本号
#   2. 修改 MESSAGE 为本次更新说明
#   3. 运行 ./scripts/linux/release.sh
#
# 发布流程:
#   1. 脚本会检查当前项目版本号一致性
#   2. 如果版本不同，自动更新所有配置文件
#   3. 运行完整测试（必须全部通过）
#   4. Git 提交 + 创建标签 + 推送
#
# ============================================

VERSION=1.1.26
MESSAGE=更新进度条不再卡 0%（总长未知时走不定态滚动条而非钉死 0%，两端同步；根因是 store 边界用 || 把 undefined 吃成 0）+ 点引用块定位不再把整个 App 顶上去（改手算消息列表容器 scrollTop，不再用会沿祖先链冒泡的 scrollIntoView）+ 会话内消息查找（群聊/好友/bot 全覆盖，文字/文件/视频/图片四分类，本地 FTS5 SQL 层过滤）+ 右上角选项栏由下拉改为侧边滑出并按语义重新分组（危险操作恒在最后）。顺带修掉三个既有缺陷：菜单项 transition:all 与 motion 抢帧、隐藏菜单项仍在 Tab 序且 Enter 仍触发、测试用 .at(-1) 超出 TS lib target。已知未做：bot/好友私聊回复（后端 friend-messages 表无 reply-to 列，需跨仓改动）
