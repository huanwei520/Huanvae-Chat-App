# ============================================
# Huanvae Chat App 发布配置
# ============================================
#
# 使用方法:
#   1. 修改 VERSION 为要发布的版本号
#   2. 修改 MESSAGE 为本次更新说明
#   3. 运行 ./scripts/linux/release.sh
#
# 发布流程:
#   1. 脚本会检查当前项目版本号一致性
#   2. 如果版本不同，自动更新所有配置文件
#   3. 运行完整测试（必须全部通过）
#   4. Git 提交 + 创建标签 + 推送
#
# ============================================

VERSION=1.1.47
MESSAGE=修Guard打包门禁（Windows安装包缺HuanvaeGuard组件根治）：v1.1.46 起安装包不含 HuanvaeGuard/huanvaeguard-svc.exe 与 wintun.dll（06e52282 置空 tauri.windows/macos.conf.json externalBin 时把平台 conf 的 bundle.resources 映射一并删除），安装后 sc start HuanvaeGuard 恒返回 2（文件不存在）、App 内修复服务失败。本版：①恢复并钉住映射——tauri.windows.conf.json 恢复 resources/HuanvaeGuard/* → HuanvaeGuard/，tauri.macos.conf.json 恢复 resources/HuanvaeGuard-macos/* → HuanvaeGuard-macos/，tauri.conf.json 基础 resources 增补 resources/HuanvaeGuard/* → HuanvaeGuard/（合并语义依据 tauri-cli 2.11.1 json_patch::merge 即 RFC 7396 对象深合并；externalBin 置空维持不变）；②门禁加闸——新增 scripts/linux/assert-artifact-content.sh 并接入 test-all.sh 第 14 步：静态腿（映射并集+源文件在仓非空）恒跑、产物腿对已构建安装包逐个解包核对必含文件（7z 解 NSIS/dpkg-deb 解 deb/AppImage 自解/unzip 查 APK），缺任一必含文件退出码 1；实机自证：对 v1.1.38 真安装包 PASS、对带病 v1.1.46 真安装包 FAIL（拦住本次事故形态）；tests/winService.nsisContract.test.ts 补 4 用例（映射并集 Windows/macOS 腿+Notification-Sounds 保留+源文件非空），负向验证红→还原绿；③vpn-connectivity 项 Linux 宿主结构性不可执行（hg-connectivity-test.sh 仅覆盖 macOS launchctl 链路，uname 判非 Darwin 即退出码 3），登记式放行+如实报告，VPN 真实握手/通流改由本版发布后在 winserver-hg 真机验收（sc start 返回 0+隧道握手+通流）。
