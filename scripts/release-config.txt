# ============================================
# Huanvae Chat App 发布配置
# ============================================
#
# 使用方法:
#   1. 修改 VERSION 为要发布的版本号
#   2. 修改 MESSAGE 为本次更新说明
#   3. 运行 ./scripts/linux/release.sh
#
# 发布流程:
#   1. 脚本会检查当前项目版本号一致性
#   2. 如果版本不同，自动更新所有配置文件
#   3. 运行完整测试（必须全部通过）
#   4. Git 提交 + 创建标签 + 推送
#
# ============================================

VERSION=1.1.24
MESSAGE=macOS 登录不再要 Touch ID：选已保存账号登录时直接解密 App 私有 AES 文件，摘掉 get_password 里那道 LAContext 生物识别门禁；连带清理因此永不可达的死代码(StorageError::Biometric 变体、前端 biometric 错误分支)。打开 VPN 前的生物识别门禁是独立路径，原样保留不受影响。本版只含这一个功能
