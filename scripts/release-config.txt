# ============================================
# Huanvae Chat App 发布配置
# ============================================
#
# 使用方法:
#   1. 修改 VERSION 为要发布的版本号
#   2. 修改 MESSAGE 为本次更新说明
#   3. 运行 ./scripts/linux/release.sh
#
# 发布流程:
#   1. 脚本会检查当前项目版本号一致性
#   2. 如果版本不同，自动更新所有配置文件
#   3. 运行完整测试（必须全部通过）
#   4. Git 提交 + 创建标签 + 推送
#
# ============================================

VERSION=1.1.27
MESSAGE=Android 后台下载完成不再需要清后台重下（根因是上游 tauri-plugin-android-package-install 2.0.2 的 install 命令所有路径都不调 resolve/reject，await 永不 settle；改为不 await + 完成收尾全部移到 Rust，不可见时发通知，冷启动恢复待安装包）+ 私聊支持引用回复（后端 migration 036 起支持，群聊那套上提 chat/shared 复用）+ 修好「别人回复你时引用块从来不出现」：该字段要穿过 后端下发/本地落库/DB→UI 转换/WS 直推 四段，其中三段一直在丢，群聊自 v1.1.25 起即如此且因静默无报错而未被发现+ 相册（media_group）接收侧：按 media_group_id 折叠成网格、配文在整组下方、格高按 aspect-ratio 预留不跳版、跨分页缺口占位不重排+ 群聊侧边栏改为非模态且不再遮挡聊天标题栏，会话内查找并入侧边栏并支持点分类即按时间列出（文字/图片/视频/文件四分类）+ 群成员改为微信式头像网格置于侧边栏首项+ 引用定位改为平滑滚动+ 群聊卡片预览加上发送者昵称+ 修好更新下载的 UA/Accept 整形回归+ 根治 release.sh 标签时序校验（把「读失败」与「指向不符」分开，此前连续五次误判中止发布）。清理：删除已被 store 取代且零调用方的 useUpdateToast 重复状态机。已知未做：相册发送侧仅落地基（上传层/编排/合成面板均已就位但尚未接入口，行为零变化），下一版接线。
