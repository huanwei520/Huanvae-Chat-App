# ============================================
# Huanvae Chat App 发布配置
# ============================================
#
# 使用方法:
#   1. 修改 VERSION 为要发布的版本号
#   2. 修改 MESSAGE 为本次更新说明
#   3. 运行 ./scripts/linux/release.sh
#
# 发布流程:
#   1. 脚本会检查当前项目版本号一致性
#   2. 如果版本不同，自动更新所有配置文件
#   3. 运行完整测试（必须全部通过）
#   4. Git 提交 + 创建标签 + 推送
#
# ============================================

VERSION=1.1.22
MESSAGE=WS 静默永久停摆根治(连接世代计数取代粘滞布尔) + VPN 页面按 frontend-design 方法论整页重构 + 群组邀请码合一(粘一次即可加入) + 已连接设备不可再选取 + 页面可滚动 + 握手/流量统计不再把空闲显示成故障 + macOS 未公证分发首次打开指引
