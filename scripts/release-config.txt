# ============================================
# Huanvae Chat App 发布配置
# ============================================
#
# 使用方法:
#   1. 修改 VERSION 为要发布的版本号
#   2. 修改 MESSAGE 为本次更新说明
#   3. 运行 ./scripts/linux/release.sh
#
# 发布流程:
#   1. 脚本会检查当前项目版本号一致性
#   2. 如果版本不同，自动更新所有配置文件
#   3. 运行完整测试（必须全部通过）
#   4. Git 提交 + 创建标签 + 推送
#
# ============================================

VERSION=1.1.35
MESSAGE=四条 UI 修复 + CI paths 修复 + 两条判据通则。① A1 媒体不再被硬撑成统一黑边盒：宽高比落在窗口 0.4 ≤ ar ≤ 2.5 内的按原比例贴合、窗口外的仍 letterbox，阈值真值源 src/chat/shared/mediaDisplaySize.ts 的 RATIO_WINDOW=2.5（0.4 是同一常量的倒数、窗口两头由这一个常量导出，不是第二个魔数）；依据是手机竖屏截图与影院比例落窗口内、长截图与超宽落窗口外，边界处短边 128px/112px 仍在本仓已在展示的尺度内。② A3 连发消息的头像沉到组底：与最后一条气泡下沿对齐（改的是 align-self，新增声明里无 transition/transform/opacity，.bubble-avatar 本就不在动画注册表，故不触发动画门禁）。③ A2 VPN 页新增「终端下载」tab，排在群组之后，内含 1 安装 / 2 验证 / 3 接入 / 4 运行 / 5 升级 / 6 卸载六段命令与用法，尖括号内一律占位符。④ V3 VPN 页关闭再打开后选中态不丢：从守护进程回读，且用 prev ?? 保留用户手选（断开后仍保持）。⑤ CI：.github/workflows/test.yml 的 8 处路径前缀改为仓相对（原前缀在仓内零命中，该 workflow 对 push 从未触发过；同批删掉 defaults 整块，仓根即默认工作目录）。⑥ .claude/rules/common.md 新增通则「负对照必须自证会产生不同形状的输出」，并就地更正「假 sha 负对照应给 422」——实测证伪：格式合法但不存在的 sha 返回 200 + 空，与真负向逐字同形、零判别力；有判别力的是同端点只换 sha 的同类正对照，以及把 workflow 文件名/仓名换成不存在的（404，形状不同）。验证与如实披露：A1/A3/V3 属布局类，jsdom 里 getBoundingClientRect() 恒 0、vitest 结构上测不出，数值正确性由 macOS 真机对比图承担——四条各自的改前/改后帧已拍齐并经独立复核（20 条关键帧 19 满足 / 1 部分满足 / 0 不满足），A1 三种比例的分档在单帧内可见（近方形零黑边与极端比例仍 letterbox 并存），V3 另带两条负对照帧（隧道未跑 / 断开后再关再开，均一台设备都不选）以证明回读真的在读守护进程。🔴 未验证项，不写成已验收：手机端与 Windows 桌面端零真机证据；A1 的「视频」那一半零真机帧（图片/视频/在途占位在代码上共用同一个尺寸函数，行为应当一致，但那是读码推的）；A3「对方那一侧（头像在左）」的改后图仍缺；A2 与 V3 无改前图（A2 改前那版是三个 tab，由 git 核出、不是拍的）。另：视觉基线 e2e 有 9 条 toHaveScreenshot 存量红，已定性为基线过期（根因 commit=6a61f58 删掉登录页「服务器地址」字段，卡片矮约 98px 并重新居中），本次未动基线、未跑 --update-snapshots，修红另立单。
