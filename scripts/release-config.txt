# ============================================
# Huanvae Chat App 发布配置
# ============================================
#
# 使用方法:
#   1. 修改 VERSION 为要发布的版本号
#   2. 修改 MESSAGE 为本次更新说明
#   3. 运行 ./scripts/linux/release.sh
#
# 发布流程:
#   1. 脚本会检查当前项目版本号一致性
#   2. 如果版本不同，自动更新所有配置文件
#   3. 运行完整测试（必须全部通过）
#   4. Git 提交 + 创建标签 + 推送
#
# ============================================

VERSION=1.1.48
MESSAGE=热修复发货 VPN guard 新二进制（根治 v1.1.47 发布早于新件产出 11 分钟、渠道包携带旧件 huanvaeguard-svc.exe fa1e0f68(08-28 构建)与当前服务端不兼容致 Windows VPN 不可用）。落点实证：Windows huanvaeguard-svc.exe=6bc85eb0（scripts/build-hg-binaries.ps1 @winserver PS1_EXIT=0）、macOS hg-macos=617f9d02（scripts/build-hg-binaries.sh @macos-14 run 35003209658），源锚 HuanvaeGuard@b1d7c8df 含 0909 daemon.rs:672 修复，hg-build-manifest.json 双腿溯源在仓。本版同时修复 d79efcea 误删 hg-macos（a0c03276 manifest 记双腿但树缺文件，将致 macOS 构建腿与门禁第14步静态腿红）——自发货内容 e9898dc2 blob 913fc82c 恢复（sha256=617f9d02 与 manifest 一致）。验证：test-all 14 项全量（第14步产物内容断言真跑）；发布后对 GitHub Release 渠道包解包复算包内 guard 哈希（必须=6bc85eb0）+ winserver-hg 真机装包验收（sc start HuanvaeGuard 返回0+隧道握手+通流+热更新横幅稳定≥60s）。步骤3 在 Linux 宿主按登记惯例 HG_BINARIES_SKIP_REGISTERED=1（macOS 腿本宿主不可交叉构建），发货件=d79efcea resources 落点原样（sha256 逐一复算与 manifest 一致，本版要发货的正是这对新件）；vpn-connectivity Linux 宿主结构性不可执行（hg-connectivity-test.sh 非 Darwin 即退出码3），登记式放行+如实报告，VPN 真实握手/通流由本版发布后 winserver-hg 真机验收补足。
