# ============================================
# Huanvae Chat App 发布配置
# ============================================
#
# 使用方法:
#   1. 修改 VERSION 为要发布的版本号
#   2. 修改 MESSAGE 为本次更新说明
#   3. 运行 ./scripts/linux/release.sh
#
# 发布流程:
#   1. 脚本会检查当前项目版本号一致性
#   2. 如果版本不同，自动更新所有配置文件
#   3. 运行完整测试（必须全部通过）
#   4. Git 提交 + 创建标签 + 推送
#
# ============================================

VERSION=1.1.25
MESSAGE=群聊消息回复(Telegram 式：输入框上方正在回复条 + 气泡内可点引用块 + 点击滚动定位并高亮原消息；原消息未加载时给明确降级提示而非无反应) + 移动端列表点头像改为进会话(与桌面同口径，不再跳资料页) + 移动端个人资料页支持左边缘侧滑返回 + 输入框改为按会话独立草稿并在切回时重算高度(修「切回来变成一行」) + 登录页账号卡去掉 API 地址显示 + 账号按上次登录时间倒序排列(新增 last_login_at，存量回落 created_at)且重排有过渡动画。另：把「新功能必须桌面/移动两端对齐」写成 code-review 与 blind-review 的硬性验收项
