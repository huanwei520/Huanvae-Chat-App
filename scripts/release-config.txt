# ============================================
# Huanvae Chat App 发布配置
# ============================================
#
# 使用方法:
#   1. 修改 VERSION 为要发布的版本号
#   2. 修改 MESSAGE 为本次更新说明
#   3. 运行 ./scripts/linux/release.sh
#
# 发布流程:
#   1. 脚本会检查当前项目版本号一致性
#   2. 如果版本不同，自动更新所有配置文件
#   3. 运行完整测试（必须全部通过）
#   4. Git 提交 + 创建标签 + 推送
#
# ============================================

VERSION=1.1.41
MESSAGE=v1.1.41: 版本例行 +0.0.1（owner 明令，流水线块 1789185311119-1 第 4 轮重做；前 3 轮因回合超时被杀，v1.1.40 已由第 3 轮发出，本轮自 v1.1.40 递增）。本版相对 v1.1.40 的代码增量 = 零：git diff v1.1.40^{}..HEAD 为空，纯版本递增发布。发布机 Linux x86_64（Ubuntu 24.04 内核 6.8），两处结构性差异沿 v1.1.40 登记照旧成立：① release.sh 步骤 3（build-hg-binaries.sh）在本宿主不可跑——macOS 腿硬要求 Darwin（非 Darwin 即 exit 1），Windows 腿需 HG_WIN_BUILD_HOST（无默认值、本机未注入）；故本次发货 VPN 二进制与 v1.1.40 逐字节一致：git diff v1.1.40..HEAD -- src-tauri/resources/ 为空，hg-macos/libhg_android.so 的 sha256 与 hg-build-manifest.json 对账相等。② 门禁 test-all.sh 真跑 12/13：第 13 项 VPN 连通性在 Linux 恒退 3=未执行（取数手段仅覆盖 macOS/Windows），经 ALLOW_SKIP=vpn-connectivity 显式放行并如实登记；第 11 项 Android clippy 由本机 NDK 真跑通过（未走放行）。桌面（pnpm tauri build --bundles deb --no-sign）与 Android（pnpm tauri android build --apk）双轨本机构建绿灯；签名产物由 tag 触发的 .github/workflows/release.yml 产出（gate → build×3 + build-android → manifest → R2）。🔴 未验证项，不写成已验收：第 13 项真跑 0/5；Windows/macOS 真机零验证；本机 Android APK 无发布签名（签名在 CI），CI 产物与更新清单待 tag 推出后复核。
