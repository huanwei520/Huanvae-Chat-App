# ============================================
# Huanvae Chat App 发布配置
# ============================================
#
# 使用方法:
#   1. 修改 VERSION 为要发布的版本号
#   2. 修改 MESSAGE 为本次更新说明
#   3. 运行 ./scripts/linux/release.sh
#
# 发布流程:
#   1. 脚本会检查当前项目版本号一致性
#   2. 如果版本不同，自动更新所有配置文件
#   3. 运行完整测试（必须全部通过）
#   4. Git 提交 + 创建标签 + 推送
#
# ============================================

VERSION=1.1.46
MESSAGE=v1.1.46: UI三件套落地（owner 选定方案）+ 九项修复收口。本版相对 v1.1.45 的代码增量：①按钮体系 D2 玻璃描边统一（owner 2026-09-15 选定「透明底+1px 描边+主色字」覆盖力度偏弱注记）：AppButton primary/danger 变体、会议表单按钮 .meeting-btn.primary、控制条圆钮 .control-btn、tile 控制胶囊 .tile-control-pill 与移动端 .mob-control-pill、会议邀请卡 CTA 全部归位 D2 构型；②#7 tile 控制胶囊平台能力门控（isControllablePlatform：Android/iOS 不可被控不显入口；授权弹窗 meetingBridge 按本机能力成面）+ 移动端胶囊独立交互（--open 驱动）；③#6 文件多发竖向堆叠（album-file-stack 纵排+数量徽标，废除三宫格）；④配色系统 token 归位（miniapps 渐变/markdown 代码高亮紫色硬编码清除，走 --primary-subtle/--accent-subtle/--accent-text）；⑤#9 平台徽章端到端（PlatformBadge + platform 字段上报/信令分发/渲染，Linux/Android 真端互见实证）+ Android sidecar 补 aarch64 件；⑥VPN 发货二进制沿用 2026-09-14T09:34Z cwsecdh0-1 重建产物（源 e34de18+abcc9cc，含 daemon.rs:672 修复，manifest v3 溯源在档）——本发布轮未变更 resources 落点，步骤3 因 Linux 宿主不可跑 macOS 腿按登记惯例跳过（HG_BINARIES_SKIP_REGISTERED=1）。验证（全本体退出码）：tsc --noEmit RC=0；vitest 393 文件 4386 用例全过；pnpm build RC=0；干净检出（git archive HEAD）vite build RC=0。发布机 Linux x86_64。
